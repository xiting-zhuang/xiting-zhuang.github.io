source "https://rubygems.org"

# 为了兼容GitHub Pages，使用最新版本
gem "github-pages", group: :jekyll_plugins

# 必要插件 - GitHub Pages已经包含了这些
group :jekyll_plugins do
  gem "jekyll-sitemap"
  gem "jekyll-seo-tag" 
  gem "jekyll-feed"
end